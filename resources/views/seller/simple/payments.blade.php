<x-layouts.seller title="Payments"><x-seller.header title="Payments" subtitle="Real order-payment records and seller earnings." :vendor="$vendor" /><section class="seller-content seller-table-card">@forelse ($items as $item)<div class="seller-row-link">{{ $item->order?->order_number }} · {{ str($item->order?->payment_method)->title() }} · {{ str($item->order?->payment_status)->title() }} · Gross Rs {{ number_format((int) $item->gross_line_amount) }} · Platform Fee Rs {{ number_format((int) ($item->platform_fee_total ?: $item->commission_amount)) }} · Earning Rs {{ number_format((int) $item->seller_earning) }} · {{ str($item->settlement_status)->title() }}</div>@empty<p>No seller payments yet.</p>@endforelse {{ $items->links() }}</section></x-layouts.seller>
